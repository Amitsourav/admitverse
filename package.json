{
  "name": "admitverse",
  "version": "1.0.0",
  "private": true,
  "description": "Your Gateway to International Education",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
